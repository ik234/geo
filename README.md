# Географический детектив

Статический сайт для ребёнка 9 лет: география, флаги, животные; русский, английский и немного португальского.

## Запуск

```sh
python3 -m http.server 8765 --directory dist
```

Затем открыть `http://localhost:8765`.

## Состояние

- 175 флагов в сложном уровне.
- Простой уровень сохраняет прежние 24 флага; средний добавляет расширенный, но ещё узнаваемый набор стран.
- 30 животных: 5 простых, 10 средних и 15 сложных.
- Фиксированные раунды зависят от сложности: простой — 5 загадок, средний — 15, сложный — 30.
- Есть бесконечный режим: он ограничен только размером текущего каталога животных/флагов.
- Нет таймера и штрафов.
- UI, сообщения, подсказки и базовые факты вынесены в i18n-словарь RU/EN/PT.
- Названия стран локализуются через `Intl.DisplayNames`.
- Есть режим исследования карты.
- Сборка не требуется: точка входа `dist/index.html`.
